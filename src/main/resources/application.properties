# Change 'MySQL' to your actual MySQL username (often root)
spring.datasource.username=MySQL

# Change this to your actual MySQL password
spring.datasource.password=Amypo

# Ensure the database name in the URL matches your local database
spring.datasource.url=jdbc:mysql://localhost:3306/skill_barter?createDatabaseIfNotExist=true